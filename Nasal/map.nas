var loop = func {
var lon = getprop('/position/longtitude-deg');
var lat = getprop('/position/latitude-deg');
if ( lat >= 0 and lat < 1 ) {
setprop('/instrumentation/map/filename.png', 1.png);
}
}
loop()
